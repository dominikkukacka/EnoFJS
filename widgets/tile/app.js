define('widgets/tile/app',
['widget', 'classExtentions'],
function (Widget) {
	function Tile(view, title, message) {
		var _protected = {};

		
		this._protected = _protected;
	}
	Tile = Tile.extend(Widget);
	return Tile;
});